<h1>New Song</h1>

<%= form_with(local: true) do |form| %>
  <div class="field">
    <%= form.label :source %>
    <%= form.text_field :source %>
  </div>

  <div class="actions">
    <%= form.submit("Resolve") %>
  </div>
<% end %>

<%= link_to 'Back', songs_path %>
